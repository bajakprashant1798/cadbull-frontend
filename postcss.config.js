// ✅ DEPLOYMENT-SAFE: Simplified PostCSS configuration
module.exports = {
  plugins: [
    // For now, disable PurgeCSS to ensure successful deployment
    // We can re-enable it after deployment is stable
  ],
};
